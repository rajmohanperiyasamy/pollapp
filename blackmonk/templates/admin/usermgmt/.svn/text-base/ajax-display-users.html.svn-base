{% load i18n %}  
{% load ds_utils %}
{% load truncate_filter %}
{% load thumbnail %}
  
  <!-- Hidden Fields -->
  <input type="hidden" name="view_by" id="id_view_by" value="{{view_by}}">    
  <input type="hidden" name="sort" id="id_sort" value="{{sort}}">	
  <input type="hidden" value="{% if search %}{{search}}{% endif %}" name="id_search" id="id_search">
  <input type="hidden" value="" name="id_action" id="id_action">
  <!-- Hidden Fields -->

{% if profiles %} 
  <ul class="cat-list-container">
    <li class="cat-list-row hdr-rw">
        <div class="cat-list-row-inner group">
            <div class="cat-list-column member-login">
            	{% trans 'Login' %}                                                
            </div>
            <div class="cat-list-column member-display-name">
            	{% trans 'Display As' %}
            </div>
            <div class="cat-list-column member-last-login">
            	{% trans 'Last Login' %}
            </div>
            <div class="action-buttons">
            </div>
            <div class="member-status">
            </div>                                                
		</div>
	</li> 
	<div id="id_append_list"> <!-- Extra Div Shaan -->
 	{% for profile in profiles %}
		<input type="hidden" class="dev_allids" value="{{profile.id}}">
        <li class="cat-list-row bm-profile-list" id="id_user_list_{{profile.id}}">
        
        <span class="checkbox-wrap">
			<span class="cform-checkbox-container"><input type="checkbox" value="{{profile.id}}" id="id_user_action" name="artCulture" class="check-overflow cbox"><span class="cform-checkbox-element"></span></span>
		</span>
        
	        <div class="cat-list-row-inner group">
	            <div class="cat-list-column member-login">
	            	<a href="{% url 'admin_ajax_user_profile' %}?uid={{profile.id}}" title="{% trans 'Profile Information' %}" class="bm-add-gadget light_box">
	                	<div class="user-icon">
						{% if profile.image %}
						<img src="{% thumbnail profile.image 48x48 %}" class="inline-block o3"></div>
						{% else %}
							<img src="{{STATIC_URL}}images/generic.gif" width="48" height="48" class="inline-block o3"></div>
						{% endif %}
	                	<h5 class="username">{{profile.display_name}}</h5>
	                	<span class="email" title="{{profile.email}}">{{profile.useremail|truncate_chars:28}}</span>
					</a>                                                    
	            </div>
	            <div class="cat-list-column member-display-name">
	            	<span class="display-name"><a href="javascript:void(0);">{{profile.get_full_name|default:profile.display_name}}</a></span>
	            </div>
	            <div class="cat-list-column member-last-login">
	            	<span class="time">{{profile.last_login|get_readable_time_format}} {% trans 'ago' %}</span>
	            </div>
	            <div class="action-buttons">
	              	<div class="group">
	                    <!--a title="{% trans 'Delete User Account' %}" class="tttxt-n" onclick="if(confirm(gettext('Are you sure you want to delete this user?'))){delete_content({{profile.id}});return false;}" href="javascript:void(0);"><span class="action-icon icon-delete"></span></a-->
						<a href="{% url 'admin_ajax_delete_type' profile.id %}" target="_blank" class="btn-txt bm-add-gadget" title="Delete / Deactivate User"><span class="action-icon icon-delete"></span></a>
	                    <a title="{% trans 'Update User Account' %}"  href="{% url 'admin_ajax_update_user' %}?uid={{profile.id}}" class="txt-button tttxt-n bm-add-gadget light_box"><span class="action-icon icon-edit"></span></a>
					</div>                                                            
	            </div>
	            <div class="member-status">
	                <div class="relative-block dev_hide_list_status" onclick="change_status({{profile.id}});" id="id_change_status_div_{{profile.id}}">
	                    <span class="status-inside inline-block" id="id_change_status_span_{{profile.id}}">
							<span class="inline-block status-idty {% if profile.status == 'A' %}icon-active-user{% else %}icon-inactive-user{% endif %}" id="id_change_status_span_icon_{{profile.id}}"></span>
							<span class="arrow"></span>
						</span>
	                    <ul class="dropdown-list" id="id_change_status_ul_{{profile.id}}">
	                        <li><strong>{% trans 'Update status to...' %}</strong></li>
	                        <li><a href="javascript:void(0)" id="id_change_status_text_{{profile.id}}" onclick="save_status({{profile.id}});">{% if profile.status == 'A' %}{% trans 'Inactive' %}{% else %}{% trans 'Active' %}{% endif %}</a></li>
							<li><a title='Promote to Staff' href="{% url 'admin_ajax_promote_user' profile.id %}" id="id_promote_link_{{profile.id}}" class="bm-add-gadget light_box" style="{% if profile.status == 'B' %}display: none;{% endif %}">{% trans 'Promote' %}</a></li>
	                    </ul>  	 
	                </div>                                                     
	            </div>                                                
			</div>
		</li> 
	{% endfor %}
    </div> <!-- Extra Div Shaan Ends-->
  </ul>
  <div id="pagenation">{% include 'admin/usermgmt/ajax-pagination.html' %}</div> 
{% else %}
<div class="no-data"> <p>{% trans 'Sorry , No users found !' %}</p></div>
{% endif %}
<script type="text/javascript" src="{{STATIC_URL}}ui/js/inline.js"></script>
<script>
	$(document).ready(function(){
	$(".bm-add-gadget").colorbox({width: "560",initialWidth: "560", top:"5%",initialHeight: "200",title:function(){return getLBTitle($(this));}});
			})
</script>