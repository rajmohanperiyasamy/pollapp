{% load i18n %}
{% if attributeall %}
<ul class="cat-list-container" id="group">
	{% for cat in attributeall %}
	<li class="cat-list-row  collaps-view carousal-list" id="append_{{cat.id}}">
    	<div class="cat-list-row-inner group">
            <div class="cat-list-column cat-name"><h3>{{cat}}</h3></div>
            <div class="cat-list-column action-buttons">
            	<div class="group">
                    <a href="{% url 'admin_portal_business_attributes_update' %}?gid={{cat.id}}&cid={{cid}}" title="{% trans 'Add Attribute' %}" class="tttxt-n bm-add-gadget"><span class="icon icon-edit"></span></a>
                </div>                                                            
			</div>
			<div class="cat-list-column item-view-toggle">
              <a href="#" class="collapse-view">Expand View</a>
            </div>
		</div>
		<ul class="sub-cat-list-container" id="append_atr_{{cat.id}}">
		{% for subcat in parentattrbutes %}
		{% ifequal subcat.attribute_group cat %}
            <li class="sub-cat-list-row" id="atr_{{subcat.id}}">
            	<div class="sub-cat-list-row-inner group">
                    <div class="cat-list-column cat-name">
                        <h3>{{subcat}}- ({{subcat.category}})</h3>
                    </div>
                </div>                                                    
            </li>
		{% endifequal %}
		{% endfor %} 
		{% for subcat in attrbutes %}
		{% ifequal subcat.attribute_group cat %}
            <li class="sub-cat-list-row" id="atr_{{subcat.id}}">
            	<div class="sub-cat-list-row-inner group">
                	<span class="checkbox-wrap">
						<span class="cform-checkbox-container"><input type="checkbox" value="{{subcat.id}}" id="sub{{subcat.id}}" name="sub" class="check-overflow"><span class="cform-checkbox-element"></span></span>
					</span>
                    <div class="cat-list-column cat-name">
                        <h3>{{subcat}}</h3>
                    </div>
                    <div class="cat-list-column action-buttons">
                        <div class="group">
                            <!--a title="{% trans 'View Stats' %}" class="tttxt-n" href="#"><span class="icon icon-stats"></span></a-->
                            <a href="{% url 'admin_portal_business_attributes_update' %}?id={{subcat.id}}&gid={{cat.id}}&cid={{cid}}" title="{% trans 'Update Attribute' %}" class="tttxt-n bm-add-gadget"><span class="icon icon-edit"></span></a>
                            <a title="{% trans 'Delete' %}" class="tttxt-n" href="javascript:delete_attr({{subcat.id}})"><span class="icon icon-delete"></span></a>
                        </div>                                                           
                    </div>
                </div>                                                    
            </li>
		{% endifequal %}
		{% endfor %} 
		</ul>  
	</li>  
	{% endfor %}                                           
</ul> 
{% else %}
<ul class="cat-list-container" id="group">
	<li class="cat-list-row  collaps-view carousal-list">
		<div class="cat-list-row-inner group">
			Please Choose Category To View/Add/Update Attrributes.
		</div>
	</li>
</ul>
{% endif %}           