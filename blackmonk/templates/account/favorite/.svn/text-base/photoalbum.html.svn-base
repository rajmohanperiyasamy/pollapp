{% load thumbnail %}
{% load i18n %}
{% load template_loop %}	
	<div class="preview-items-head">
     <h4>{% trans 'Favorite Photos' %}</h4>
    </div>
	{% if gallery %}		   
	<ol class="data-table table-lists-container" id="ajax_content_list">
			{% for gal in gallery %}	
			<li class="table-list" id="li_{{gal.id}}">
			        <div class="table-list-container has-thumb110 group">
			            <div class="item-media">
                        	<a href="{{ gal.get_absolute_url }}" class="inline-block">
                                <span class="item-thumb thumb-110">
                                    <span class="thumb-in">
                                        <span class="thumb"><img src="{% if gal.get_cover_image %}{% thumbnail gal.get_cover_image 110x0 %}{% else %}{% if gal.get_cover_url %}{{ gal.get_cover_url }}{% else %}{{gal.get_default_image}}{% endif %}{%endif%}" alt="{{gal.title}}" class="inline-block"><span class="inline-block h100"></span></span>
                                    </span>
                                </span>
							</a>                                                            
                        </div> 
						<div class="item-info relative-block">
			                <h3 class="title"><a href="{{ gal.get_absolute_url }}">{{gal.title}}</a></h3>
			                <p class="ltxt80">{% trans 'On' %} {{gal.created_on|date:"d M, Y"}}</p>
			                <ul class="quick-stats group">
			                    <li class="cmnt">{% autoescape off %}{{ gal.summary|truncatewords:40  }}{% endautoescape %}</li>
			                </ul>
			            </div>
			        </div>
			    </li> 
			{% endfor %}
			</ol>
			<div id="pagenation">
				{% include 'account/favorite/pagenation.html' %}      
				<input type="hidden" id="url" value="{% url 'ajax_myfav_list' 'photoalbum' %}">
				<input type="hidden" id="module" value="photoalbum">    
			</div>  
			{% else %}
				<div class="no-data"> <p>{% trans 'Sorry , No favorite photos found' %}</p></div>
         	{% endif %}
