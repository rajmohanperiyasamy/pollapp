{% load ds_utils %}
{% load i18n %}
<div class="signal-body">
        {% if nots %}
	    <ul>
			{% for n in nots %}
				{% if n.content_type.name == 'user' %}
					<li>
		                <span class="bold"><a href="{% url 'user_profile' n.user.profile_slug %}">"{{ n.user.get_full_name }}"</a></span> {{n.notification_type}} <span class="bold"><a href="{{ globalsettings.website_url }}">{{ globalsettings.domain }}</a></span>
		                <span class="sub_title">
		                    {{n.timestamp|get_readable_time_format}} {% trans 'ago' %}
		                </span>
	            	</li>
				{% else %}
		            <li>
		                <span class="bold">{% if n.content_object %}<a href="{{ n.content_object.get_staff_preview_url }}">"{{n.content_object|truncatechars:30 }}"</a>{% else %}{% if n.object_title %}<a href="javascript:void(0)">"{{ n.object_title }}"</a>{% else %}<a href="javascript:void(0)">"{% trans 'No Name' %}"</a>{% endif %}{% endif %}</span> {{n.notification_type}} <span class="bold"><a href="{{ n.content_object.get_staff_listing_url }}">{% get_notify_module n.content_type.name %}</a></span>
			                <span class="sub_title">
			                    <span class="by_line">{% trans 'by' %} <a href="{% url 'user_profile' n.user.profile_slug %}">{{ n.user.get_full_name }}</a></span>
			                    {{n.timestamp|get_readable_time_format}} {% trans 'ago' %}
			                </span>
		            </li>
				{% endif %}
            {% endfor %}
	        
        </ul>
		{% else %}
			<span id="no_todo_found" class="no-item"><p>No notifications found</p></span> 
		{% endif %}	             
    </div><!--signal-body-->  