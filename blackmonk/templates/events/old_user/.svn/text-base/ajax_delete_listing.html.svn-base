{% load ds_utils %}
{% load i18n %}		
{% load thumbnail %}	
{% if eventsdisplay %}
	{% for event in eventsdisplay %}	
	{% for ni in new_id %}
	{% ifequal ni event.id %}	
			<li class="table-list" id="li_{{event.id}}">
			        <div class="table-list-container check-item has-thumb110 group">
			        	{% ifequal event.listing_type 'F'%}
			        		<span class="list-type featured-item"><a class="tttxt" title="{% trans 'Item listed as featured' %}" href="#">{% trans 'Featured' %}</a></span>
				 		{% else %}
						{% ifequal event.listing_type 'S' %}
							<span class="list-type sponsored-item"><a class="tttxt" title="{% trans 'Item listed as sponsored' %}" href="#">{% trans 'Sponsored' %}</a></span>
				  		{% endifequal %}
				 		{% endifequal %}
						<span class="checkbox-wrap">
							<span class="cform-checkbox-container"><input type="checkbox" value="{{event.id}}" id="id_event_action" name="artCulture" class="check-overflow cbox"><span class="cform-checkbox-element"></span></span>
						</span>
			            <div class="item-media">
                        	<a href="{% url 'events_addevent' %}?eid={{event.id}}" class="inline-block">
                                <span class="item-thumb thumb-110">
                                    <span class="thumb-in">
                                        <span class="thumb"><img src="{% if event.get_first_event_photo %}{% thumbnail event.get_first_event_photo 110x0 %}{% else %}{{event.get_default_image}}{%endif%}" alt="{{event.title}}" class="inline-block"><span class="inline-block h100"></span></span>
                                    </span>
                                </span>
							</a>                                                            
                        </div> 
						<div class="item-info relative-block">
			                <h3 class="title"><a href="{% url 'events_addevent' %}?eid={{event.id}}">{{event.title}}</a></h3>
			                <p class="ltxt80">{% if event.category.all %}{% for cat in event.category.all %}{{cat.name}}{% if not forloop.last %}, {% endif %}{% endfor %}{% endif %} <span class="divider12">|</span> {% trans 'By' %} <a href="{% url 'user_profile' event.created_by.profile_slug %}">{{event.created_by.profile.get_full_name}}</a> <span class="divider12">|</span>{{event.created_on|date:"M d, Y"}}</p>
			                <ul class="quick-stats group">
			                    <li class="views"><a class="tttxt" href="javascript:void(0)" title="{% trans 'Viewers' %}"><span class="inline-block icon-view-stats"></span>{{event.visits}}</a></li>
			                    <!--li class="cmnt"><a class="tttxt"title="{% trans 'View comments' %}" href="#"><span class="inline-block icon-cmnt-stats"></span></a></li-->
			                </ul>
			                <div class="item-status no-options dev_hide_list_status">
			                	<span class="status-inside inline-block" id="id_span_chge_sts_{{event.id}}">
                                
								<span title="{% get_status_class event.status %}" name="{{event.status}}" id="id_estatus_{{event.id}}" class="inline-block tttxt status-idty icon-{% get_status_class event.status %}"></span>	 
                                
			                    <!--span class="arrow"></span--></span>
								<!--ul class="dropdown-list" id="change_status_id_{{event.id}}">
									<li><strong>{% trans 'Update status to...' %}</strong></li>
									<ul id="id_dis_status_{{event.id}}">
			                       	</ul>
			                    </ul-->
			                </div>                                                        
			            </div>
			            <div class="action-buttons {% if forloop.counter == 1 %}tip tip6{% endif %}">
			                <div class="relative-block">
			                    <span class="item-manage inline-block dev_hide_list_settings" id="id_span_settings_{{event.id}}" onclick="display_settings({{event.id}});"><span class="action-icon icon-manage"></span></span>
			                    <ul class="dropdown-list dev_settingsdrpdwn" id="settings_id_{{event.id}}" style="display:none">
			                       	{% if event.status == 'P' and event.listing_type != 'F' %}
										<li><a title="{% trans 'Upgrade Listig Type' %}" href="{% url 'events_ajax_payment' event.id %}" class="bm-business-listing">{% trans 'Upgrade Listing Type' %}</a></li>
										<li><span class="line-break"></span></li>
									{% else %}
										{% if event.status == 'D' %}
											<li><a title="{% trans 'Send for Approval' %}" href="{% url 'events_ajax_payment' event.id %}" class="bm-business-listing">{% trans 'Send for Approval' %}</a></li>
											<li><span class="line-break"></span></li>
										{% endif %}
									{% endif %}
			                        <li><a href="{% url 'events_addevent' %}?eid={{event.id}}">{% trans 'Edit' %}</a></li>
									<li><a title="{% trans 'Title / Description (SEO)' %}" href="{% url 'events_update_seo' event.id %}" class="bm-add-gadget light_box">{% trans 'Update SEO' %}</a></li>
			                        <li><span class="line-break"></span></li>
			                        <li><a onclick="if(confirm(gettext('Are you sure you want to delete the selected event?'))){filter_action({{event.id}},'DEL');return false;}" href="javascript:void(0)">{% trans 'Delete' %}</a></li>
			                    </ul>
							</div>                                                              	                                                         
			            </div>
			        </div>
			    </li>
			{% endifequal %}
		 {% endfor %}
		 {% endfor %}
		 {% else %}
		 	<div class="no-data"> <p>{% trans 'Sorry , No events found !' %}</p></div>
 {% endif %}  	 